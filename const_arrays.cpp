#include "enums.hpp"
#include "types.h"
#include "constants.hpp"

namespace cvc {

const char latDim_names[] = { 't', 'x', 'y', 'z' };
const char shift_dir_names[] = { 'f', 'b' };
const mom_t zero_mom{0, 0, 0};

} // namespace(cvc)
